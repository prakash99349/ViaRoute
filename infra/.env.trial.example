# Settings for infra/docker-compose.trial.yml. Copy to .env.trial (repo root) and fill in.
# Never commit the real file.

# Your server's public IP, e.g. 203.0.113.10
SERVER_IP=

# Generate each with:  openssl rand -hex 32
POSTGRES_PASSWORD=
# openssl rand -hex 48
JWT_SECRET=
# Exactly 64 hex characters:  openssl rand -hex 32
ENCRYPTION_KEY=
